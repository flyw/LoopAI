# 01 — deterministic greeting

**Status:** ready-for-agent

## Dependencies

**Blocked by:** None

Required dependency inputs:
- examples/spec.md

## Current subtask

Implement a deterministic `greet(name)` function using the target repository's language and
conventions.

## Definition of Done

- `greet("Ada")` returns `Hello, Ada!` through the production interface.
- Empty names are rejected with the conventional validation error.
- Relevant existing tests and new boundary tests pass.

